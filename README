Classe de creation et de manipulation d'elements html via le DOM,
sans pretention, c'est surtout un gros raccourcis qui evite la repetition de code.
De plus cela permet de deporter la charge de generation de l'interface du serveur vers le client, pour permettre des montées en charge faciles en envoyant les données brute au client qui se chargera de les interpreter.

QUICK START

instancier la classe :
var jsi=new JSI;

creer un element :
var elt=jsi.e('element',['nom_attribu_1','nom_attribu_2',...],['valeur_attribu_1','valeur_attribu_2',...],"innerHTML de l'element");

inserer un element creer :
document.getElementById('parent').appendChild(elt);
ou si vous avez plusieurs elements :
jsi.insert([elt1,elt2,...],document.getElementById('parent'));

des fonctions precoder existe pour generer certain type d'elements, reporter vous au fichier jsinterface.js pour le detail de fonctions disponibles

Des suggestions, des bugs, etc... ? Vous voulez participez ? : 
N'hesiter pas a nous rejoindre sur https://github.com/DimitriGilbert/jsinterface
